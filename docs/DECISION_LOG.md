# RecoverAI — Decision Log

| ID | Date | Decision | Reason | Alternatives Considered | Status |
|---|---|---|---|---|---|
| DEC-001 | 2026-08-24 | Frozen 61-Step Roadmap & Modular Monolith | Ensures deterministic execution and avoids over-engineering for Buildathon. | Microservices, Kafka event mesh | APPROVED |
| DEC-002 | 2026-08-24 | REAL_TEST Execution Boundary | `POST /v1/payment_links` is the sole verified Razorpay recovery API endpoint. Controlled failure trigger uses `APP_EVENT: PAYMENT_FAILED`. | Unverified charge retries | APPROVED |
| DEC-003 | 2026-08-24 | Tamper-Evident SHA-256 Audit Trail | Cryptographic hash chaining anchored to `GENESIS_HASH` provides verifiable integrity without misleading immutability claims. | Append-only DB without hashing | APPROVED |
| DEC-004 | 2026-08-24 | Safe Execution Protocol Adoption | Adopting strict single-step verification, control documents, and git rollback safety. | Ad-hoc step execution | APPROVED |
| DEC-005 | 2026-08-26 | LLM Provider Selection: Groq API | Project owner selected Groq API (Free tier) with initial model `llama-3.3-70b-versatile`. Groq operates behind existing AI service abstraction with strict safety boundaries. | OpenAI API | APPROVED |
| DEC-006 | 2026-08-26 | Groq Model Selection: groq/compound-mini | Project owner explicitly approved `groq/compound-mini` model following live verification confirmation of HTTP 200 availability on the active Groq key. | llama-3.3-70b-versatile (404 model_not_found) | APPROVED |
| DEC-007 | 2026-08-26 | Step 4 Split: Customer-Group Partitioning (HUMAN-APPROVED ENGINEERING DECISION) | Hard zero customer ID overlap constraint across 70/15/15 Train/Val/Test split, with internal deterministic sorting by `created_at` and `transaction_id`. | Strict temporal split (causes entity leakage), un-ordered customer split | APPROVED |
| DEC-008 | 2026-08-26 | Step 3 Historical Action Policy (SYNTHETIC SIMULATION ASSUMPTION) | Scenario-conditioned synthetic historical policy: `PAYMENT_FAILURE` (45% `PAYMENT_LINK`, 35% `RETRY`, 20% `STOP`), `CHECKOUT_ABANDONMENT` (50% `RECOVERY_MESSAGE`, 30% `PAYMENT_LINK`, 20% `STOP`), `SUBSCRIPTION_FAILURE` (40% `SUBSCRIPTION_RECOVERY`, 40% `RETRY`, 20% `STOP`), `OVERDUE_RECEIVABLE` (60% `PAYMENT_LINK`, 25% `RECOVERY_MESSAGE`, 15% `STOP`). Strictly independent of outcomes/latent $P^*$. | Uniform random assignment, outcome-dependent assignment | APPROVED |
| DEC-009 | 2026-08-26 | Action Support Measurement Policy (HUMAN-APPROVED ENGINEERING DECISION) | Measure and report action counts across splits without row padding, duplication, or label manipulation; log audit warnings for low support. | Row padding/duplication, SMOTE oversampling | APPROVED |
| DEC-010 | 2026-08-29 | Step 13B Architecture Refactoring: GroqLLMService High-Level API Abstraction | Restored service-layer encapsulation by adding generate_structured_recommendation() to GroqLLMService and refactoring StructuredAIRecommender to call it instead of accessing private/low-level SDK client directly. | Direct .client SDK call (bypasses service abstraction), merging ActionRecommendation with AIRecommendationResponse | APPROVED |
| DEC-011 | 2026-08-29 | Step 13C State Machine & Idempotency Scope Verification | Confirmed that logical_operation_key applies to downstream financial/operational execution (Step 17). Step 13C safely rejects invalid repeated lifecycle re-entry via StateTransitionService transition matrix validation. | Permitting self-transitions (INTERVENTION_SELECTED->INTERVENTION_SELECTED), introducing artificial logical operation keys in Step 13C | APPROVED |
| DEC-012 | 2026-08-29 | Step 14 Capability Resolver Execution Mode & Fallback Architecture | Implemented CapabilityResolver positioned strictly between AI Recommender and Policy Engine. Restricts REAL_TEST executable actions to PAYMENT_LINK and internal actions (STOP, ESCALATE, RECOVERY_MESSAGE). Unsupported AI recommendations in REAL_TEST automatically fall back to the highest ENRV-ranked executable candidate action or STOP. Zero direct external API calls or status mutations. | AI bypassing capability checks, allowing unverified gateway retries in REAL_TEST | APPROVED |
| DEC-013 | 2026-08-29 | Step 15 Deterministic Policy Engine Architecture & Rule Hierarchy | Implemented PolicyEngine positioned strictly between CapabilityResolver and ActionExecutor. Enforces deterministic rule hierarchy (Global -> Merchant -> Context) covering max recovery attempts (<=3), amount cap (<=₹50,000), minimum probability (>=0.15), and cooldown window (>=24h). Orchestrates atomic state transitions (INTERVENTION_SELECTED -> POLICY_CHECK -> APPROVED/STOPPED/ESCALATED) via StateTransitionService and persists RecoveryAttempt records. | AI overriding policy guardrails, direct state mutation without StateTransitionService | APPROVED |
| DEC-015 | 2026-08-29 | Step 17 Action Executor Boundary & Logical Operation Key Idempotency | Implemented ActionExecutor service to enforce an air-gapped execution boundary. Validates APPROVED state gate, defensive capability re-check via CapabilityResolver, constructs logical_operation_key (merchant_id:tx_id:cycle:action), checks for idempotent replay (returning existing execution response with is_duplicate=True), handles DB UNIQUE index collisions, dispatches mode-based execution (REAL_TEST/SIMULATION), maps adapter timeouts/exceptions to UNKNOWN execution status (preserving EXECUTING state for reconciliation), and mutates state to EXECUTING via StateTransitionService with SHA-256 audit chaining. Deferred concrete Razorpay SDK integration to Step 18. | Direct adapter calls without execution boundary, optimistic state mutation on UNKNOWN result, omitting defensive capability re-check | APPROVED |
| DEC-017 | 2026-08-29 | Step 18 Monetary Conversion & Traceability Remediation | Implemented Decimal-safe monetary conversion (`round(Decimal * 100)`) in RazorpayAdapter to prevent floating-point representation drift. Added payment link ID and reference ID persistence into RecoveryAttempt via ActionExecutor for complete end-to-end webhook traceability. | Truncating floating-point numbers, omitting link ID persistence | APPROVED |
| DEC-018 | 2026-08-29 | Step 19 Result Processor Architecture & Step 20 Attribution Hook Boundary | Implemented ResultProcessor service to process asynchronous execution outcomes (Razorpay webhooks / simulation events). Matches events via `razorpay_payment_link_id`, `razorpay_reference_id`, or `external_resource_id`. Atomically mutates transaction status via StateTransitionService (`EXECUTING` -> `RECOVERED` for paid/captured, `EXECUTING` -> `FAILED` for cancelled/failed, `EXECUTING` -> `EXPIRED` for expired). Enforces strict multi-tenant merchant isolation, unlinked event safety without unhandled exceptions, and idempotent duplicate event handling (`IDEMPOTENT_SKIPPED`). Established clean async callback hook boundary for Step 20 AttributionEngine integration without premature implementation. | Processing results outside StateTransitionService, allowing cross-tenant webhook mutation, throwing unhandled exceptions for unlinked events | APPROVED |
| DEC-020 | 2026-08-29 | Step 20 Attribution Engine Hierarchy & Idempotent Persistence | Implemented AttributionEngine service establishing a deterministic 4-tier classification hierarchy (DIRECT_REFERENCE -> WINDOW_MATCH -> NATURAL_RECOVERY -> UNATTRIBUTED). Enforces multi-tenant merchant isolation, DB UNIQUE constraint `(transaction_id, recovery_attempt_id)` for idempotent outcome insertion, preservation of `recovery_source` (`REAL_TEST` vs `SIMULATION`), zero transaction state mutation, and registered hook integration with `ResultProcessor`. | Heuristic/probabilistic LLM attribution, mutating transaction status during attribution evaluation | APPROVED |
| DEC-021 | 2026-08-29 | Step 21 Control/Treatment Measurement Engine Architecture | Implemented MeasurementEngine service and analytics schemas to calculate incremental recovery lift and net business ROI comparing Treatment cohorts against Baseline Control cohorts. Enforces strict Decimal financial precision (`Decimal` rounding), zero-division resilience, multi-tenant merchant isolation, `REAL_TEST` vs `SIMULATION` mode separation, zero transaction state mutation, and automatic persistence of summary metrics to `evaluation_runs` database table. | Floating-point arithmetic rounding, mixing SIMULATION and REAL_TEST cohorts, mutating transaction status during measurement | APPROVED |
| DEC-023 | 2026-08-29 | Step 23 Continuous Audit Trail & Canonical JSON Serialization | Implemented AuditTrailService and canonical_json serializer to enforce continuous cryptographic SHA-256 hash chaining across all system lifecycle events. Enforces transaction-level row locking (`select(Transaction.id).where(...).with_for_update()`) prior to hash retrieval to prevent concurrent audit chain forks. Anchors genesis events to `GENESIS_HASH` (`hashlib.sha256('RECOVERAI-AUDIT-GENESIS-V1')`). Provides `verify_chain(transaction_id)` to cryptographically validate chain integrity and pinpoint tampered records. Integrated with StateTransitionService while maintaining zero breaking changes to existing audit event schemas. | Non-deterministic JSON serialization, mutable audit logs without hash chaining, lockless audit logging susceptible to chain forks | APPROVED |
| DEC-025 | 2026-08-30 | Step 25 REST API Architecture & Endpoints Implementation | Implemented Step 25 REST API layer with FastAPI routers for Transactions (`/api/v1/transactions`), Analytics (`/api/v1/analytics/summary`), Audit (`/api/v1/audit`, `/api/v1/audit/verify`), Policies (`/api/v1/policies`), and Evaluations (`/api/v1/evaluations`). Enforced Pydantic input/output schemas (`backend/app/schemas/transaction.py`, `backend/app/schemas/audit.py`, `backend/app/schemas/policy.py`, `backend/app/schemas/evaluation.py`), multi-tenant merchant isolation via query parameters and `X-Merchant-ID` headers, read-only GET safety (zero direct state mutations), bounded pagination (`1..100`), strict Decimal money serialization, mode separation (`SIMULATION` vs `REAL_TEST`), partial policy update semantics (`PATCH /api/v1/policies/{policy_id}`), and cryptographic hash chain verification via `AuditTrailService`. Created full 33-scenario test suite in `backend/tests/test_rest_apis.py` achieving 100% pass rate (289/289 across workspace). | Direct SQL state mutations in endpoints, bypassing StateTransitionService/MeasurementEngine/AuditTrailService, unisolated cross-tenant data access, unvalidated pagination limits | APPROVED |
| DEC-026 | 2026-08-30 | Step 26 JWT Auth, RBAC & Multi-Tenant Isolation Security Layer | Implemented authoritative security layer in `backend/app/core/security.py` supporting JWT Bearer tokens and `X-API-Key` headers. Established `AuthenticatedIdentity` dependency (`get_current_identity`), `get_current_merchant` tenant dependency, and `require_role` RBAC dependency. Enforced strict RBAC roles (`ROLE_ADMIN`, `ROLE_MERCHANT`, `ROLE_HUMAN_REVIEWER`). Transformed client-supplied merchant filtering to identity-scoped tenant isolation where client-supplied header/query parameters cannot override `identity.merchant_id`. Enforced cross-tenant HTTP 404 responses for unauthorized resource access. Registered `/api/v1/auth/login` and `/api/v1/auth/me` endpoints. Created 21-scenario test suite in `backend/tests/test_security.py` and updated regression suite (310/310 passing tests). | Trusting client-supplied X-Merchant-ID when authenticated identity is available, returning HTTP 403 on cross-tenant resource lookup (leaking resource existence), unauthenticated access to protected endpoints | APPROVED |
| DEC-027 | 2026-08-30 | Step 27 React TypeScript & TailwindCSS Frontend Foundation Architecture | Implemented React 18 TypeScript frontend architecture in `frontend/` using Vite, TailwindCSS (dark-mode aesthetic palette with glassmorphism design system tokens), React Router v6 SPA navigation, global navigation sidebar for all 9 dashboard pages, header bar featuring active execution mode badge (`REAL_TEST` vs `SIMULATION`), active merchant context selector, API client service (`frontend/src/services/api.ts`) with Axios request/response interceptors (injecting Bearer JWT, API keys, and merchant headers), React Error Boundary (`frontend/src/components/ErrorBoundary.tsx`), 9 placeholder page component frames, and Vitest component test suite (`frontend/src/__tests__/Layout.test.tsx`). Verified clean compilation (`npm run build`) with zero TypeScript/Vite errors and 100% backend/frontend test pass rate (310 backend + 5 frontend). | Unstyled default components, missing mode switch/merchant selection UI, hardcoded API endpoints without interceptors, monolithic page routing without Layout framing | APPROVED |
| DEC-028 | 2026-08-30 | Step 28 Command Center Executive Performance Dashboard | Implemented `CommandCenter` page in `frontend/src/pages/CommandCenter.tsx` and reusable `MetricCard` in `frontend/src/components/MetricCard.tsx`. Created 5 executive KPI stat cards (Revenue at Risk, Recovered Revenue, Incremental Lift, Net Recovery ROI, Recovery Rate), Recharts Treatment vs Baseline Control recovery comparison chart, active execution mode toggle badge (`REAL_TEST` / `SIMULATION`), recent active recovery queue summary table with direct inspection links, skeleton loading states, empty queue states, and resilient fallback error handling when backend telemetry drops. Created Vitest component test suite in `frontend/src/__tests__/CommandCenter.test.tsx` achieving 100% pass rate (8/8 frontend + 310/310 backend tests passing). | Unstyled metric displays, hardcoded static numbers without backend API integration, missing treatment vs control comparison chart, missing mode toggle in main dashboard | APPROVED |
| DEC-029 | 2026-08-30 | Step 29 Revenue Risk Exposure Monitoring Dashboard | Implemented `RevenueRisk` page in `frontend/src/pages/RevenueRisk.tsx` displaying at-risk transactions. Created filterable scenario class tabs (`ALL`, `PAYMENT_FAILURE`, `CHECKOUT_ABANDONMENT`, `SUBSCRIPTION_LAPSE`, `BANK_DOWNTIME`, `INSUFFICIENT_FUNDS`), real-time search bar filtering by Transaction ID or Customer Email/Name, risk exposure summary cards (Total Value At Risk, High/Critical Severity Count, Active Tenant), direct inspection links to Transaction Detail view (`/transactions/:id`), risk level badges (`CRITICAL`, `HIGH`, `MEDIUM`, `LOW`), empty queue state banner handling ("No revenue currently at risk"), and Vitest test suite in `frontend/src/__tests__/RevenueRisk.test.tsx` achieving 100% pass rate (12/12 frontend + 310/310 backend tests passing). | Unfilterable risk queues, missing search bar capability, unhandled empty queue UI states, client-side XSS vulnerabilities in search inputs | APPROVED |
| DEC-030 | 2026-08-30 | Step 30 Recovery Queue Intervention Dashboard | Implemented `RecoveryQueue` page in `frontend/src/pages/RecoveryQueue.tsx` displaying active intervention cycles and automated workflows. Created active interventions data table (Transaction ID, Action/Intervention Type, Logical Operation Key, Status, Attempt & Cycle Numbers, Timestamp), color-coded status badges (`EXECUTING`, `SUCCESS`, `UNKNOWN`, `FAILED`), manual queue refresh trigger button (`Refresh Queue`), attempt retry count and cycle displays, pagination controls (`Page X of Y`, Previous/Next buttons, limit dropdown), loading skeleton loader, empty queue state banner, resilient fallback mock dataset, and Vitest component test suite in `frontend/src/__tests__/RecoveryQueue.test.tsx` achieving 100% pass rate (18/18 frontend + 310/310 backend tests passing). | Missing status color coding, missing pagination for large queues, missing manual queue refresh trigger, unhandled empty queue state | APPROVED |
| DEC-031 | 2026-08-30 | Step 31 Transaction Detail Closed-Loop Lifecycle Dashboard | Implemented `TransactionDetail` page in `frontend/src/pages/TransactionDetail.tsx` and visual stepper component `LifecycleStepper` in `frontend/src/components/LifecycleStepper.tsx`. Visualizes end-to-end recovery lifecycle flow (`DETECT` -> `DIAGNOSE` -> `DECIDE` -> `EXECUTE` -> `VERIFY` -> `ATTRIBUTE` -> `MEASURE` -> `AUDIT`). Features overview header banner (ID, Customer, Amount, Status, Mode), 6-stage closed-loop `LifecycleStepper` with dynamic step highlighting, root cause diagnosis panel (explanation, failure code/category, confidence score, classifier source), AI recommendation & policy breakdown panel (action strategy, logical operation key, Razorpay payment link demo trigger), inline cryptographic audit timeline with SHA-256 event hash copy action, 404 transaction not found warning view with back button, and Vitest test suite in `frontend/src/__tests__/TransactionDetail.test.tsx` achieving 100% pass rate (24/24 frontend + 310/310 backend tests passing). | Missing visual lifecycle stepper, missing root cause explanation panel, missing cryptographic SHA-256 event hash copy trigger, unhandled 404 transaction lookup states | APPROVED |
| DEC-032 | 2026-08-30 | Step 25 Omitted API Dependency Correction: GET /api/v1/ai-decisions/{id} | Implemented read-only REST endpoint `GET /api/v1/ai-decisions/{id}` (in `backend/app/api/v1/endpoints/ai_decisions.py`) and schema `AIDecisionResponse` (`backend/app/schemas/ai_decision.py`) to expose persisted `DecisionContext`, `RecoveryActionScore` ENRV calculations, root cause diagnosis, and AI recommendation rationale. Enforces strict multi-tenant merchant isolation (HTTP 404 for cross-tenant access), authentication, zero state mutations, zero DB schema changes, zero Razorpay calls, and 7 dedicated unit/security tests in `backend/tests/test_ai_decisions_endpoint.py` (317/317 backend tests passing). | Modifying frozen implementation plan, mutating DB state, combining backend correction with Step 32 frontend build | APPROVED |
| DEC-035 | 2026-08-31 | Step 34 Audit Center & Cryptographic Hash Chain Verifier | Implemented `AuditCenterPage` in `frontend/src/pages/AuditCenter.tsx` and interactive `ChainVerifierWidget` in `frontend/src/components/ChainVerifierWidget.tsx`. Enables continuous inspection of immutable SHA-256 event audit log ledger and cryptographic hash chain integrity validation. Features top summary cards (Total Audit Events count, SHA-256 hash algorithm, Chain Integrity status, Genesis Anchor hash), embedded interactive `ChainVerifierWidget` consuming `GET /api/v1/audit/verify` with instant green `CHAIN VALID` badge or red `TAMPER DETECTED` alert banner, paginated audit events table (Event ID, Event Type, Actor, Transaction ID, Previous Hash with copy button, Event Hash with copy button, Timestamp, Payload inspect button), event type filter dropdown (`ALL`, `STATE_CHANGE`, `POLICY_DECISION`, `EXECUTION`, `DIAGNOSIS`, `RECOVERY_ATTEMPT`, `ATTRIBUTION_MEASURE`), search bar filtering by Transaction ID, canonical JSON inspector modal displaying full untruncated SHA-256 hash strings and event detail payloads, execution mode switch toggle (`SIMULATION` / `REAL_TEST`), and Vitest component test suite in `frontend/src/__tests__/AuditCenter.test.tsx` achieving 100% pass rate (43/43 frontend + 317/317 backend tests passing). | Truncating SHA-256 hash strings in inspection modals, client-side re-hashing instead of backend API verification, missing event type filters, missing JSON payload modal | APPROVED |
| DEC-036 | 2026-08-31 | Step 35 Policy Manager Dashboard & Merchant Guardrail Controls | Implemented `PolicyManagerPage` in `frontend/src/pages/PolicyManager.tsx` providing a centralized interface for configuring merchant recovery guardrails, maximum attempt caps, cooldown windows, and ML recovery probability floors. Features executive KPI summary cards (Active Policy Version, Max Retries Cap, Max Auto-Action Amount Cap in INR, Min ML Probability Floor, Cooldown Window in Hours), interactive policy rules editor form consuming `GET /api/v1/policies` and `PATCH /api/v1/policies/{id}`, automated policy version string increment (`v1.0` -> `v1.1`), read-only global safety bounds reference panel (Hard Attempt Ceiling of 5, Global Amount Limit of ₹100,000.00, Minimum Probability Floor of 5.0%, Minimum Cooldown Floor of 1 Hour, Mandatory Human Escalation Trigger), animated success toast notification banner, inline client-side numerical bounds validation, offline API fallback resilience, and Vitest test suite in `frontend/src/__tests__/PolicyManager.test.tsx` achieving 100% pass rate (48/48 frontend + 317/317 backend tests passing). | Unvalidated numerical form inputs, client-side policy modification without backend PATCH synchronization, missing global safety ceiling reference panel, missing policy version indicator | APPROVED |
| DEC-037 | 2026-08-31 | Step 36 Human Review Dashboard & Operator Authorization Control | Implemented `HumanReviewPage` in `frontend/src/pages/HumanReviewPage.tsx` and `ReviewModal` in `frontend/src/components/ReviewModal.tsx` displaying escalated transactions requiring manual operator authorization. Features top KPI summary cards (Pending Escalations, High-Value Escalations >=₹10k, Approved Overrides, Terminated Actions), review item cards grid highlighting escalation reason code badges, filter tabs (`PENDING`, `APPROVED`, `REJECTED`, `ALL`), real-time search bar filtering by Transaction ID or Merchant ID, "INSPECT & ACTION" trigger button opening `ReviewModal`, decision action triggers `APPROVE_OVERRIDE` (force execution) and `REJECT_PERMANENT` (terminate) submitted to `POST /api/v1/human-review/items/{review_id}/decision`, dynamic queue card status update, inline error alert banner in modal on API failure while preserving card state, offline mock queue fallback resilience, and Vitest test suite in `frontend/src/__tests__/HumanReview.test.tsx` achieving 100% pass rate (56/56 frontend + 317/317 backend tests passing). | Decision triggers without reviewer authorization field, closing modal on backend error (losing input state), missing escalation reason code badges on queue cards, missing dynamic card status updates | APPROVED |
| DEC-038 | 2026-09-01 | Step 37 Comprehensive Unit Testing & Audit Reconciliation | Verified and augmented targeted unit test suite under `backend/tests/unit/` for ENRV calculator formulas, policy engine rule threshold boundaries, state machine transition matrix completeness, and continuous SHA-256 canonical audit hash chaining. Reconciled test count evolution (317 baseline tests -> 344 baseline unit discovery -> 353 full unit suite tests). Verified 100% pass rate across 36 unit tests, 353 total Pytest backend tests, 56 Vitest frontend component tests, and clean Vite TypeScript production build. | Skipping standalone unit test layer, modifying frozen implementation plan, unverified test count assertions | APPROVED |
| DEC-039 | 2026-09-01 | Step 38 Database & Integration Testing Suite | Implemented comprehensive database & integration test modules in `backend/tests/integration/test_database_integration.py` and `backend/tests/integration/test_redis_locks.py`. Verified physical PostgreSQL/SQLite database guarantees: UNIQUE(logical_operation_key) rejection on duplicate recovery action attempts, safe application-level duplicate handling via `ActionExecutor` returning `is_duplicate=True`, UNIQUE(razorpay_event_id) / UNIQUE(idempotency_key) duplicate webhook rejection, SELECT ... FOR UPDATE multi-session row locking in `StateTransitionService` preventing racing state corruptions, physical metadata verification across all 13 core relational tables, Redis fast-path deduplication cache hits with 24h TTL key registration, and Redis outage fallback to PostgreSQL ACID boundary preserving state correctness without duplicate financial actions. Verified 100% pass rate across 7 integration tests, 360 total Pytest backend tests, 56 Vitest frontend component tests, and clean Vite production build. | Replacing DB integration tests with mocks, bypassing SELECT FOR UPDATE row locking, unhandled Redis connection outages, modifying frozen implementation plan | APPROVED |
| DEC-040 | 2026-09-01 | Step 39 ML Evaluation Testing Suite & Offline Report | Implemented offline ML model evaluation test suite in `backend/tests/ml/test_ml_evaluation.py` and comprehensive report in `docs/ML_EVALUATION_REPORT.md`. Evaluated Action-Conditional Recovery Model on held-out test partition (`data/test.parquet`, 7,535 rows): verified ROC-AUC = 0.7934 (exceeds frozen Step 12 threshold >= 0.75), PR-AUC = 0.8351, Brier Score = 0.1595 (meets threshold <= 0.20), Log-Loss = 0.5037, Accuracy = 0.7858, Precision = 0.7746, Recall = 0.9410, F1 = 0.8498, and confusion matrix. Evaluated Multi-Class Diagnosis XGBoost Classifier and verified probability output distribution constraints (sum = 1.0). Confirmed zero customer overlap (seed 42 split), zero target leakage (decision-time features only), candidate action sensitivity, and offline read-only execution safety (zero Razorpay API calls, zero state mutations). Verified 100% pass rate across 5 ML tests, 365 total Pytest backend tests, 56 Vitest frontend component tests, and clean Vite production build. | Retraining on test set, altering test labels, threshold tuning against test set, target leakage, modifying frozen implementation plan | APPROVED |
| DEC-041 | 2026-09-01 | Step 40 AI Structured-Output Testing Suite | Implemented structured AI output test suite in `backend/tests/ai/test_ai_schemas.py` and package init `backend/tests/ai/__init__.py`. Verified Pydantic JSON schema parsing for `AIRecommendationResponse` and `ActionRecommendation` models against valid LLM payloads, missing required fields (raising `ValidationError`), out-of-bounds/invalid field types (confidence score > 1.0 or < 0.0), malformed JSON resiliency (unquoted keys, trailing commas, truncated text), and candidate action enum validity enforcement. Verified PII sanitization in `StructuredAIRecommender.sanitize_context` and `DiagnosisEngine.sanitize_payload_for_llm` redacting sensitive keys (`email`, `phone`, `contact`, `card`, `cvv`, `secret`, `password`, `token`, `key`) and patterns (`@`, 10-16 digit numbers). Verified deterministic fallback to top ENRV-ranked action upon Groq LLM API timeout/exception, unconfigured API key, or unsupported action rejection (`confidence_score=0.50`). Verified air-gapped security boundary ensuring AI recommender operates exclusively as a pure advisory layer with zero capability to invoke Razorpay APIs, mutate transaction status, or bypass capability/policy engines. Verified 100% pass rate across 12 targeted AI tests, 377 total Pytest backend tests, 56 Vitest frontend component tests, and clean Vite production build. | Bypassing schema validation, allowing raw PII to cross LLM prompt boundary, allowing LLM output to directly execute financial actions, modifying frozen implementation plan | APPROVED |











